import { ApplyTemplatePipe } from './apply-template.pipe';

describe('ApplyTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
